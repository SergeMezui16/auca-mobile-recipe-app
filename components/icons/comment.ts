import { MessageCircleMoreIcon } from 'lucide-react-native';

import { iconWithClassName } from '@/lib/utils';

iconWithClassName(MessageCircleMoreIcon);
export { MessageCircleMoreIcon as CommentIcon };
